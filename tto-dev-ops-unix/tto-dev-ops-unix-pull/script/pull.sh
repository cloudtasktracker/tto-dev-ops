#!/usr/bin/env bash
cd .. &&
cd .. &&
cd .. &&
# TTL-COMMONS
cd ttl-commons &&
git pull &&
# TTA-API
cd .. &&
cd tta-api &&
git pull &&
# TTF-GUI
cd .. &&
cd ttf-gui &&
git pull &&
# TTF-LANDING
cd .. &&
cd ttf-landing &&
git pull &&
# TTS-TASK
cd .. &&
cd tts-task &&
git pull &&
# TTS-TRACK
cd .. &&
cd tts-track &&
git pull &&
# TTF-ACCEPTANCE
cd .. &&
cd ttf-acceptance &&
git pull